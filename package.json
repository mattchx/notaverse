{
  "name": "notaverse",
  "version": "1.0.0",
  "description": "Full stack application with concurrent client and server development",
  "scripts": {
    "install:all": "cd client && npm install && cd ../server && npm install",
    "dev": "concurrently \"cd client && pnpm run dev\" \"cd server && npm run dev\"",
    "client": "cd client && pnpm run dev",
    "server": "cd server && npm run dev"
  }
}